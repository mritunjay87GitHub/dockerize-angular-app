
#upstream backend {
#    server backend1:8080;  # This name should match the service name
#}

server {
    listen 8083;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

     #location /api/ {
     #   proxy_pass http://backend1:8080/;
     # }

  ##  location /service2/ {
  ##     proxy_pass http://backend2:8080/;
  ## }
}
